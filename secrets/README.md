# Access Keys not stored here

This repo intentionally does **not** contain `.env`, `auth.json`, OAuth tokens, SSH keys, cookies, Telegram bot tokens, provider API keys, or GitHub tokens.

To restore on a new VPS, copy `env.template` to `~/.hermes/.env` and fill values from your password manager / original secure source.
